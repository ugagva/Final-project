import React from 'react';
import {Buttons} from "../Buttons";

const RegisterPage = () => {
    return (
        <div >
            <Buttons name={'Cancel'}></Buttons>
            <Buttons  name={'Register'}></Buttons>
        </div>
    );
};

export default RegisterPage;